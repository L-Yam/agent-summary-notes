## 00:41 - 清点本机 Codex skills

- 干了什么：检查了 `C:\Users\33055\.codex\skills` 和 `C:\Users\33055\.codex\skills\.system` 下当前存在的 skill 目录，并提取可用 `SKILL.md` 标题。
- 执行要求/标准：以后用户询问本机已有 skills 时，优先读取真实本地目录，而不是只复述当前会话上下文；同时区分用户安装 skills、系统内置 skills 与插件提供 skills。
- 关键文件/位置：`C:\Users\33055\.codex\skills`；`C:\Users\33055\.codex\skills\.system`。
- 验证结果：已通过 PowerShell 实际列目录确认，共看到 16 个用户目录项，其中 15 个有 `SKILL.md`；系统内置 skills 共 5 个。
- 后续建议：如需确认每个 skill 的用途，可继续批量读取各自 `SKILL.md` 的描述段落并生成一份表格。

## 00:44 - 删除 paper 开头的 Codex skills

- 干了什么：删除了 `C:\Users\33055\.codex\skills` 下所有名称以 `paper` 开头的 skill 目录，共 12 个，包括 `paper-spine` 及其 audit/build/citation/humanize/intake/latex/research/rewrite/translate/ui/update 子 skill。
- 执行要求/标准：执行递归删除前必须先解析并校验目标路径，确认所有删除对象都位于预期 skills 根目录内；删除后必须复查残留数量。
- 关键文件/位置：`C:\Users\33055\.codex\skills\paper-spine*`；PowerShell `Remove-Item -LiteralPath ... -Recurse -Force`。
- 验证结果：删除后再次扫描 `paper*` 目录，`REMAINING_COUNT=0`。
- 后续建议：如需恢复 PaperSpine，可使用对应安装或更新 skill/插件重新安装，而不是手工拼回目录。

## 01:09 - 确认 Computer Use 插件能力

- 干了什么：读取并按 `computer-use` 技能流程初始化 Windows 控制能力，执行轻量 `list_apps` 检查，确认可以获取当前 Windows 应用与窗口列表。
- 执行要求/标准：以后用户询问或请求 Windows 桌面自动化时，先按插件技能初始化并用轻量窗口列表检查可用性；确认成功后再进行点击、输入、截图或应用控制。
- 关键文件/位置：`C:\Users\33055\.codex\plugins\cache\openai-bundled\computer-use\26.527.31326\skills\computer-use\SKILL.md`；`scripts/computer-use-client.mjs`；Node REPL `sky.list_apps()`。
- 验证结果：验证成功，返回 `appCount=40`，并能看到 Cherry Studio、微信、Clash Verge、抖音、Telegram、Codex++ 管理工具等正在运行窗口。
- 后续建议：实际操作用户应用前，应先确认目标应用和窗口，避免误操作隐私或无关窗口。
